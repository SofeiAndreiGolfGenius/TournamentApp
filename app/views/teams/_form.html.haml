= form_with(model: @team, local: true) do |f|
  = render 'shared/error_messages', object: @team
  = f.label :name
  = f.text_field :name, class: 'form-control'
  = f.submit yield(:submit_button_text), class: 'btn btn-primary gray_button'
