<!DOCTYPE html>
<html>
  <head>
    <title>TournamentApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", {media: "all",
                                            "data-turbo-track": "reload"} %> <!-- Declara sa foloseasca css-ul application din assets-->
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% flash.each do |message_type, message| %>
      <div class="alert alert-<%= message_type %>">
        <%= message %>
      </div>
    <% end %>
    <%= yield %>
  </body>

  <%= debug(params) if Rails.env.development? %> <!-- Restrictioneaza informatiile de debug in environmentul de development, sa nu apara la useri-->
</html>
